# vpc_subnet_pub - Public Subnet

## Overview
This Terraform module provisions a **Public Subnet** within the **Production VPC** (`vpc_main`).
It creates all necessary networking components including route tables, internet gateway,
and optional network ACLs to enable public internet connectivity.

## Architecture